---
// Not a current destination. Terry's August 2026 region list is Scotland and
// Ireland only. The route is kept so existing links and the sitemap history
// still resolve; the page body was unreachable dead code behind this redirect
// and carried seven stock image URLs, so it was removed.
return Astro.redirect('/destinations', 301);
---
